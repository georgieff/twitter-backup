language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules #Cache node_modules for faster builds
script:
  - npm run build #Creats a production build
deploy:
  provider: heroku
  local_dir: dist
  api_key:
    secure: $heroku_token
